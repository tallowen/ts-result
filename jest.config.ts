const config = {
    preset: 'ts-jest',
    verbose: true,
    rootDir: 'src',
    transform: {},
    resolver: 'jest-ts-webcompat-resolver',
    testEnvironment: 'node',
  };
  
  module.exports = config;
  